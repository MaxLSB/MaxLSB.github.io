---
import BaseLayout from "../../components/layout/BaseLayout.astro";
import BaseHero from "../../components/shared/BaseHero.astro";
import ArticleCard from "../../components/ui/ArticleCard.astro";
import Container from "../../components/ui/Container.astro";
import ContentList from "../../components/shared/ContentList.astro";
---

<BaseLayout title="About Me" description="Learn more about my background and experiences">
  <Container>
    <BaseHero title="About Me" description="Learn more about my background and experiences" />
    
    <p class="intro-text">
      I'm a machine learning researcher working on post-training for LLMs and VLMs. I graduated from Télécom SudParis and pursued the IASD Master's program, hosted by ENS Ulm and Paris Dauphine–PSL, where I also took several shared courses from the MVA Master at ENS Paris-Saclay.
    </p>
    
    <!-- Experiences Section -->
    <section class="section">
      <h2>Experiences</h2>
      <ContentList>
        <ArticleCard
          title="AI Research Intern"
          description="LightOn"
          url="#"
          logo="/images/LightOn.png"
          dateRange="April 2025 - ongoing"
          clickable={false}
        />
        <ArticleCard
          title="AI Research Intern"
          description="Fondazione Bruno Kessler"
          url="#"
          logo="/images/FBK.png"
          dateRange="June 2024 - September 2024"
          clickable={false}
        />
      </ContentList>
    </section>

    <!-- Education Section -->
    <section class="section">
      <h2>Education</h2>
      <ContentList>
        <ArticleCard
          title="Master in Machine Learning"
          description="IASD - ENS-Ulm & Paris Dauphine-PSL"
          url="#"
          logo="/images/IASD.png"
          dateRange="2024 - 2025"
          clickable={false}
        />
        <ArticleCard
          title="Master in Machine Learning"
          description="MVA - ENS Paris-Saclay (Cross-enrollement)"
          url="#"
          logo="/images/MVA.png"
          dateRange="2024 - 2025"
          clickable={false}
        />
        <ArticleCard
          title="MSc in Statistics"
          description="Télécom SudParis"
          url="#"
          logo="/images/TSP.png"
          dateRange="2021 - 2024"
          clickable={false}
        />
        <ArticleCard
          title="Preparatory Classes MPSI - MP*"
          description="Fénelon-Sainte-Marie"
          url="#"
          logo="/images/FSM.png"
          dateRange="2019 - 2021"
          clickable={false}
        />
      </ContentList>
    </section>
  </Container>
</BaseLayout>

<style>
  /* Prevent layout shift by using CSS custom properties */
  :root {
    --scrollbar-width: 0px;
  }

  /* Calculate scrollbar width and store it */
  html {
    scrollbar-gutter: stable both-edges;
  }

  /* Force consistent layout by preventing horizontal overflow */
  body {
    overflow-x: hidden;
    min-width: 100vw;
  }

  /* Ensure the main container doesn't shift */
  .container {
    width: 100%;
    max-width: 768px;
    margin: 0 auto;
    box-sizing: border-box;
  }

  /* Prevent any horizontal movement */
  html, body {
    position: relative;
    left: 0;
    right: 0;
  }

  .intro-text {
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--text-muted);
    margin: 1.5rem 0 2.5rem 0;
    text-align: left;
    max-width: none;
  }

  .section h2 {
    margin-bottom: 1rem;
  }

  .section + .section {
    margin-top: 3rem;
  }
</style>

<script>
  // Calculate scrollbar width to prevent layout shift
  const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
  document.documentElement.style.setProperty('--scrollbar-width', `${scrollbarWidth}px`);
</script>

