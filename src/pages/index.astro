---
import { getEntry } from "astro:content";
import BaseLayout from "../components/layout/BaseLayout.astro";
import Container from "../components/ui/Container.astro";
import SocialIcon from "../components/SocialIcon.astro";

const siteConfig = await getEntry("site", "home");
if (!siteConfig) {
  throw new Error("Site configuration not found");
}

const site = siteConfig.data;
---

<BaseLayout title={site.name} description="Maxence Lasbordes - AI/ML Researcher">
  <Container>
    <section class="hero">
      <div class="profile-picture">
        <img 
          src="/images/pp_max.jpg" 
          alt="Maxence Lasbordes" 
          loading="eager"
          fetchpriority="high"
          width="200"
          height="200"
        />
      </div>
      <h1>{site.name}</h1>
      
      {
        site.socialLinks && (
          <div class="social-links">
            {site.socialLinks.map(({ platform, url }: { platform: string; url: string }) => (
              <a href={url} target="_blank" rel="noopener noreferrer" class="social-link">
                <SocialIcon platform={platform} size={platform.toLowerCase() === 'hugging face' ? 18 : 14} />
                <span>{platform}</span>
              </a>
            ))}
          </div>
        )
      }
      
      <div class="affiliations">
        <div class="affiliation">
          <img 
            src="/images/LightOn.png" 
            alt="LightOn" 
            class="affiliation-logo" 
            width="48"
            height="48"
          />
          <a href="https://www.lighton.ai/" target="_blank" rel="noopener noreferrer" class="affiliation-text">@LightOn</a>
        </div>
        <div class="affiliation">
          <img 
            src="/images/kurakurai-logo.png" 
            alt="KuraKurai" 
            class="affiliation-logo" 
            width="48"
            height="48"
          />
          <a href="https://huggingface.co/kurakurai" target="_blank" rel="noopener noreferrer" class="affiliation-text">@KuraKura AI</a>
        </div>
      </div>
    </section>
  </Container>
</BaseLayout>

<style>
  main {
    width: 100%;
    max-width: 768px;
    margin: 0 auto;
    padding: 2rem 0;
    overflow-x: hidden;
  }

  .hero {
    padding: 1.5rem 0 3rem 0;
    text-align: center;
    overflow-x: hidden;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 8rem);
    transform: translateY(-6rem);
  }

  .profile-picture {
    margin-bottom: 1rem;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .profile-picture img {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
  }

  h1 {
    font-size: 1.5rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
    color: var(--text-color);
    letter-spacing: -0.02em;
  }

  .subtitle {
    font-size: 1.25rem;
    font-weight: 300;
    margin-bottom: 1.5rem;
    color: var(--text-color);
    letter-spacing: -0.01em;
  }


  .social-links {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 1.5rem;
    overflow-x: hidden;
    max-width: 100%;
    padding: 0 1rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    color: var(--text-color);
    text-decoration: none;
    font-size: var(--text-xs);
    padding: 0.375rem 0.5rem;
    border-radius: 0.25rem;
    border: 1px solid var(--border-color);
    background: var(--bg-color);
    white-space: nowrap;
    flex-shrink: 0;
    min-height: 2rem;
    transition: color var(--link-animation-duration) ease;
    transform: none;
  }

  .social-link:hover {
    color: var(--link-color);
    transform: none;
  }

  .social-link span {
    position: relative;
    transition: color var(--link-animation-duration) ease;
    font-size: var(--text-sm);
  }

  .social-link span::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: currentColor;
    transition: width var(--link-animation-duration) ease;
  }

  .social-link:hover span::after {
    width: 100%;
  }

  .affiliations {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
    margin-top: 1rem;
    flex-wrap: wrap;
    padding: 0 1rem;
  }

  .affiliation {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: var(--text-sm);
    line-height: 1;
  }

  .affiliation-text {
    color: var(--text-muted);
    text-decoration: none;
    transition: color var(--link-animation-duration) ease;
    display: flex;
    align-items: center;
    line-height: 1;
    height: 48px;
  }

  .affiliation-text:hover {
    color: var(--link-color);
  }

  .affiliation-logo {
    width: 48px;
    height: 48px;
    object-fit: contain;
    opacity: 0.85;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @media (max-width: 640px) {
    main {
      padding: 1.5rem 0;
      min-height: calc(100vh - 3rem);
    }

    .hero {
      padding: 0.5rem 0 2rem 0;
      min-height: calc(100vh - 3rem);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      transform: translateY(-12rem);
    }

    .profile-picture {
      margin-bottom: 0.75rem;
    }

    .profile-picture img {
      width: 160px;
      height: 160px;
    }

    h1 {
      font-size: 1.25rem;
      margin-bottom: 0.5rem;
      text-align: center;
    }

    .subtitle {
      font-size: 1.1rem;
      text-align: center;
    }

    .social-links {
      gap: 0.5rem;
      padding: 0 0.5rem;
      margin-top: 0.75rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .social-link {
      padding: 0.25rem 0.375rem;
      font-size: var(--text-xs);
      min-height: 1.75rem;
      flex-shrink: 0;
    }

    .social-link span {
      font-size: var(--text-xs);
    }

    .affiliations {
      gap: 0.75rem;
      margin-top: 0.75rem;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      padding: 0 0.5rem;
      flex-wrap: wrap;
    }

    .affiliation {
      font-size: var(--text-xs);
      justify-content: center;
      flex-wrap: nowrap;
      white-space: nowrap;
      flex-shrink: 0;
      line-height: 1;
    }

    .affiliation-text {
      font-size: var(--text-xs);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      max-width: 180px;
      height: 32px;
      line-height: 1;
      flex-shrink: 0;
    }

    .affiliation-logo {
      width: 32px;
      height: 32px;
      flex-shrink: 0;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 0.5rem 0 1.5rem 0;
      min-height: calc(100vh - 2rem);
      transform: translateY(-11rem);
    }

    .profile-picture img {
      width: 140px;
      height: 140px;
    }

    h1 {
      font-size: 1.125rem;
    }

    .social-links {
      gap: 0.375rem;
      padding: 0 0.25rem;
    }

    .social-link {
      padding: 0.2rem 0.3rem;
      font-size: 0.7rem;
      min-height: 1.5rem;
    }

    .social-link span {
      font-size: 0.7rem;
    }

    .affiliations {
      gap: 0.5rem;
      padding: 0 0.25rem;
    }

    .affiliation {
      font-size: 0.7rem;
      line-height: 1;
    }

    .affiliation-text {
      font-size: 0.7rem;
      max-width: 150px;
      height: 28px;
      line-height: 1;
    }

    .affiliation-logo {
      width: 28px;
      height: 28px;
    }
  }

  @media (max-width: 360px) {
    .affiliations {
      flex-direction: column;
      gap: 0.5rem;
      padding: 0 0.25rem;
    }
  }

  @media (max-width: 320px) {
    .affiliations {
      gap: 0.375rem;
      padding: 0 0.125rem;
    }

    .affiliation {
      font-size: 0.65rem;
      line-height: 1;
    }

    .affiliation-text {
      font-size: 0.65rem;
      max-width: 120px;
      height: 24px;
      line-height: 1;
    }

    .affiliation-logo {
      width: 24px;
      height: 24px;
    }
  }
</style>
